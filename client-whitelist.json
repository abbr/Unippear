{
    "_comment": "validClients is an array of escaped URL RegExp strings, for example [\"^https?://.*\\.?mycorp\\.com\"]",
    "_comment": "^null allows file based localhost client for debugging purpose",
    "_comment": "To allow any client, use .*",
    "validClients": [".*", "^https?://.*\\.?github\\.com", "^null"]
}
