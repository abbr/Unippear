    /*
     * validClients is an array of double escaped URL RegExp strings, 
     * for example [".*", "^https?://.*\\.?github\\.com", "^null"].
     * ^null allows file based localhost client for debugging purpose.
     * .* allows any hosts. Remove it prior to go live.
     */
[".*", "^https?://.*\\.?github\\.com", "^null"]

