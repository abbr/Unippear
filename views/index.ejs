var _SiteLayoutService;

function siteLayoutService(opts) {
  opts.headerHtmlFragmentFile = 'html/header.html';
  opts.footerHtmlFragmentFile = 'html/footer.html';
  _SiteLayoutService = opts;

  <% include require.js %>
    <% include jquery.js %>
    $.noConflict();
  jQuery(document).ready(function($) {
    $("#namex").append("f");
  });
  require.config({
    baseUrl: "<%-currHost%>/js"
  });
  require(["<%-jsFiles%>"]);
};